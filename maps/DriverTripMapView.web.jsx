/**
 * Web stub – react-native-maps is native-only. On web the activeTrip screen
 * renders MapGridBackground instead; this file prevents Metro from bundling
 * the native module on web at all.
 */
export const DriverTripMapView = () => null;
