diff --git a/node_modules/expo-keep-awake/src/index.ts b/node_modules/expo-keep-awake/src/index.ts
index b7a856e..05d2949 100644
--- a/node_modules/expo-keep-awake/src/index.ts
+++ b/node_modules/expo-keep-awake/src/index.ts
@@ -30,11 +30,15 @@ export function useKeepAwake(tag?: string, options?: KeepAwakeOptions): void {
 
   useEffect(() => {
     let isMounted = true;
-    activateKeepAwakeAsync(tagOrDefault).then(() => {
-      if (isMounted && ExpoKeepAwake.addListenerForTag && options?.listener) {
-        addListener(tagOrDefault, options.listener);
-      }
-    });
+    activateKeepAwakeAsync(tagOrDefault)
+      .catch(() => {
+        // Android: CurrentActivityNotFoundException during early dev startup (e.g. expo withDevTools).
+      })
+      .then(() => {
+        if (isMounted && ExpoKeepAwake.addListenerForTag && options?.listener) {
+          addListener(tagOrDefault, options.listener);
+        }
+      });
     return () => {
       isMounted = false;
       if (options?.suppressDeactivateWarnings) {
